"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = _default;

var _assert = _interopRequireDefault(require("assert"));

var _path = require("path");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _default(api) {
  const _api$service = api.service,
        paths = _api$service.paths,
        config = _api$service.config;
  api.register('onStart', () => {
    if (config.outputPath) {
      paths.outputPath = config.outputPath;
      paths.absOutputPath = (0, _path.join)(paths.cwd, config.outputPath);
    }
  });
  api.register('modifyConfigPlugins', ({
    memo
  }) => {
    memo.push(() => {
      return {
        name: 'outputPath',

        validate(val) {
          (0, _assert.default)(typeof val === 'string', `Configure item outputPath should be String, but got ${val}.`);
        }

      };
    });
    return memo;
  });
}