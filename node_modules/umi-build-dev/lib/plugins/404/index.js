"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = _default;

var _path = require("path");

var _lodash = _interopRequireDefault(require("lodash.clonedeep"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _default(api) {
  const paths = api.service.paths;
  const winPath = api.utils.winPath;

  if (process.env.NODE_ENV === 'development') {
    api.register('modifyRoutes', ({
      memo
    }) => {
      const notFoundRoute = {
        component: `
() => React.createElement(require('${winPath((0, _path.join)(__dirname, 'NotFound.js'))}').default, { pagesPath: '${paths.pagesPath}', routes: '${JSON.stringify(memo).replace(/\"/g, '^')}' })
        `.trim()
      };
      const routes = (0, _lodash.default)(memo);

      function addNotFound(_route) {
        if (!_route.routes) {
          return;
        }

        _route.routes.forEach(_r => addNotFound(_r));

        _route.routes.push(notFoundRoute);
      }

      routes.forEach(r => addNotFound(r));
      return routes;
    });
  }
}